query ExplorerTransferVote($id: ID!) {
  transfer(id: $id) {
    reference
    timestamp
    status
    reason
    fromAccountType
    from
    to
    toAccountType
    asset {
      id
    }
    amount
  }
}
