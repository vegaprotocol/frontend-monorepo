query ExplorerTreasury($partyId: ID!) {
  party(id: $partyId) {
    accountsConnection {
      edges {
        node {
          type
          asset {
            id
          }
          market {
            id
            state
          }
          balance
        }
      }
    }
  }
}
