query PartyStakeLinkings($partyId: ID!) {
  party(id: $partyId) {
    id
    stakingSummary {
      linkings {
        edges {
          node {
            id
            txHash
            status
          }
        }
      }
    }
  }
}
