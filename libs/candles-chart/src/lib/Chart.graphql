query Chart($marketId: ID!) {
  market(id: $marketId) {
    decimalPlaces
    data {
      priceMonitoringBounds {
        minValidPrice
        maxValidPrice
        referencePrice
      }
    }
  }
}
