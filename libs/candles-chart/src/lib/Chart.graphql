query Chart($marketId: ID!) {
  market(id: $marketId) {
    decimalPlaces
    positionDecimalPlaces
    data {
      priceMonitoringBounds {
        minValidPrice
        maxValidPrice
        referencePrice
      }
    }
  }
}
