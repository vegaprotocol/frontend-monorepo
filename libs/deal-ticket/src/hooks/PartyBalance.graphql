query PartyBalance($partyId: ID!) {
  party(id: $partyId) {
    accounts {
      ...Account
    }
  }
}

fragment Account on Account {
  type
  balance
  asset {
    id
    symbol
    name
    decimals
  }
}
