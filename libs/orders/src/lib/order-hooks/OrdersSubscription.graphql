fragment OrderSubFields on OrderUpdate {
  type
  id
  status
  rejectionReason
  createdAt
  size
  price
  timeInForce
  expiresAt
  side
  marketId
}

subscription OrderSub($partyId: ID!) {
  orders(partyId: $partyId) {
    ...OrderSubFields
  }
}
