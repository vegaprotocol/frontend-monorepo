{
  "name": "frontend-monorep-tools",
  "private": true,
  "version": "1.0.0",
  "description": "Dev deps only for tools",
  "scripts": {
    "test": "npm exec tape '*.test.mjs'",
    "test:human": "npm exec tape '*.test.mjs' | tap-arc"
  },
  "license": "MIT",
  "devDependencies": {
    "tape": "^5.6.3",
    "tap-arc": "^0.3.5"
  }
}
